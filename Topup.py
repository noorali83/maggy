class Topup:

    def __init__(self, card, amount, user, school) -> None:
        super().__init__()
        self.card = card
        self.amount = amount
        self.user = user
        self.school = school
